---
'@masonrykit/core': patch
'@masonrykit/browser': patch
'@masonrykit/react': patch
---

Add npm version, bundle-size, and MIT license badges to each package README so consumers landing on npmjs.com can see the current version and size at a glance. Also fix a stale hardcoded pnpm version in the root README badge.
